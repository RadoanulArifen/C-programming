#include<stdio.h>
int main()
{
    int num1,num2,large;
    printf("Enter two number: ");
    scanf("%d %d",&num2,&num2);

    large= (num1>num2) ? num1 : num2;
    printf("large number=%d",large);

    return 0;
}
